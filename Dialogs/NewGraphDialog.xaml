<Window x:Class="TransportGraphApp.Dialogs.NewGraphDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New graph" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" FontSize="16">Enter graph name:</Label>
        <TextBox Grid.Row="1" Margin="0,5,0,5" FontSize="16" MinWidth="100" Name="GraphNameBox" x:FieldModifier="private"></TextBox>
        <Label Grid.Row="2" FontSize="16">Add attributes</Label>
        <TabControl Grid.Row="3" Name="TabControl" x:FieldModifier="private"></TabControl>
        <WrapPanel Grid.Row="4" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Click="OkClicked" MinWidth="60" Margin="0,0,10,0">Create</Button>
            <Button IsCancel="True" MinWidth="60">_Cancel</Button>
        </WrapPanel>
    </Grid>
</Window>
