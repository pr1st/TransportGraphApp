<Window x:Class="TransportGraphApp.Dialogs.SelectGraphDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select graph"  SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListView Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,5" Height="120" MinWidth="300" Name="ListView" x:FieldModifier="private"></ListView>
        <TabControl Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Name="TabControl" x:FieldModifier="private"></TabControl>
        <WrapPanel Grid.Row="2" Grid.Column="0" HorizontalAlignment="Left" Margin="0,15,0,0" Name="ModifyListButtons"
                   x:FieldModifier="private" />
        <WrapPanel Grid.Row="2" Grid.Column="1" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button IsDefault="True" Click="OkClicked" MinWidth="60" Margin="0,0,10,0">Select</Button>
            <Button IsCancel="True" MinWidth="60">_Cancel</Button>
        </WrapPanel>
    </Grid>
</Window>
